# 実機（Raspberry Pi OS Lite）では apt で入れるのが確実です。
#   sudo apt install -y python3-pygame
# pip で入れる場合のみ、このファイルを使ってください。
#   pip3 install -r requirements.txt --break-system-packages
#
# pygame 以外の依存はありません（天気予報・時報音生成は標準ライブラリだけで
# 動き、読み上げは事前生成済みの WAV を再生するだけです）。
pygame>=2.5.0
